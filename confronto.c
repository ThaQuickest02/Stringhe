#include <stdio.h>
#define DIM 10

int main(void) {
    char s1[DIM], s2[DIM];
    int ris; //negativa se s1 precede s2, nulla se s1=s2, positiva se s2 precede s1

     scanf("%s%s", s1, s2); //leggiamo il valore delle due stringhe


    return 0;
}